import React from 'react';
import Header from '../components/Header';

const page = () => {
    return (
        <div>
            <Header>About</Header>
        </div>
    );
};

export default page;